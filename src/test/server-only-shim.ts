// Testes rodam em Node, sempre em "contexto de servidor" — o pacote
// `server-only` real lança erro incondicionalmente (ele depende da
// resolução condicional do bundler do Next.js pra virar um no-op).
export {};
